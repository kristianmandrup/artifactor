# Artefact Schema

Tries to closely mimick the Faker and other JSON schemas.
One schema for all artefacts. Any artefact is a version of a specific artefact type.
All artefacts share the same basic schema.
