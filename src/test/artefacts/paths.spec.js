const _ = require('../utils');
// to use expect:
// _.expect()

const Paths = require('../../artefacts/paths');

describe('Paths', () => {
  let name = 'contacts';
  const paths = new Paths('components', name);
})