const { test, expect } = require('./dsl');

describe('Components', () => {
  describe('DELETE item', () => {
    it('deletes a single component', () => {
      expect(1 + 1).to.eql(2);
    })    
  })
})

